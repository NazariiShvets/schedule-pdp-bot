import { allPairDayKeyboard, allPairTimeKeyboard } from "./shared.keyboard";

const createPairDaysKeyboard = allPairDayKeyboard;

const createPairTimeKeyboard = allPairTimeKeyboard;

export { createPairDaysKeyboard, createPairTimeKeyboard };
